node_modules
.DS_Store
*.local
.vite
*.tsbuildinfo

# dist/ is a build artifact, not tracked. The Pi has no Node toolchain, so the
# bundle is built on a dev machine and rsync'd to the Pi (see docs/INSTALL.md).
