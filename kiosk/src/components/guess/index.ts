/**
 * GuessConfirmCard — inline confirm card rendered below TrackInfo in
 * TrackLayout's left ShoulderColumn when a predicted-state guess is
 * active. No fixed/absolute positioning; flows inside the 400px left
 * column so album art is never obscured.
 */
export { GuessConfirmCard } from './GuessConfirmCard';
